# Agent Prompt — M5.4-02/-03/-04 Late-Result Activity Evidence

Status: non-authoritative launch wrapper; the committed and independently
audited activation is the sole source of scope, ownership, gates, and stop
conditions

Date: 2026-08-28

## Copy-paste prompt

Continue GroundLoop from current `main` on the bounded M5.4-02/-03/-04
late-result activity evidence tranche.

Before doing anything:

1. read `AGENTS.md` and its complete required document sequence;
2. read
   `docs/workstreams/m5_runtime_implementation/M5_4_02_04_LATE_RESULT_ACTIVITY_ACTIVATION.md`
   completely;
3. inspect the real checkout, branch, `HEAD`, index, expanded status,
   worktrees, active test/DB processes, and protected user-owned dirt; and
4. prove that `main` points to the externally pinned, independently audited
   activation commit whose parent and exact two-path delta satisfy the
   activation.

Then execute the activation exactly. Use its literal branches, worktrees,
five-path manifest, lane order, activity matrix, static/live gates, audit
protocol, and stop conditions. Historical R2b-R2e worktrees and handoffs grant
no ownership.

The scoped outcome is only:

- qualifying the deterministic evidence for M5.4-02;
- hardening the no-parent-refutation evidence for M5.4-03; and
- closing the missing late/inactive result falsifiers for M5.4-04.

Do not edit production source unless a new independently audited correction
activation explicitly re-manifests it. Do not touch D25, migration 017,
production PostgreSQL seal/publication, deployed providers, runtime
activation, status rows, presentations, `pyproject.toml`, or other protected
work. Do not reset, clean, stash, stage with `-a`, or hide a required failure.

Stop after the audited technical integration and post-integration focused
rerun. Report the exact evidence per row and request a separate coordinator
status reconciliation. M5.0-24, M5.4-01, M5.4-05 through M5.4-09, and all
M5.5/M5.6 rows remain unchanged unless a later separately audited gate says
otherwise.
